# CONTRIBUTING —— 贡献指南

欢迎贡献实战经验与新能力。这个技能的价值来自真实事故的沉淀，不是纸面理论——所以贡献门槛是"跑过"，不是"想过"。

## lessons.md 追加规则（最重要的一节）

经验库 `references/lessons.md` 是本技能的灵魂，按以下规则追加：

1. **准入门槛**：只收在完整轮次（≥1 轮派发→执行→收官）中实战验证过的教训；推测、最佳实践转发一律不收
2. **类目归属**：三条固定类目内追加——`一、子代理管理` / `二、验证与验收` / `三、循环治理`；不新开类目（保持扫描效率）
3. **条目格式**：一行一条 = **加粗结论**（可执行的纪律，不是现象描述）+ 实例证据（中性短语）。示例：
   ```
   | 25 | **结论式纪律**：为什么+怎么做 | 某轮实录（匿名短语） |
   ```
4. **编号连续**：在所属类目末尾续号，不插行（引用稳定）
5. **匿名化强制**：证据列不得出现真实项目名/公司/硬件型号/OS 专有命令/路径/IP/账号——用"某真实项目/外部 API/远程命令"等中性词。你的教训要能在任何领域的项目上被复用
6. **修改已有条目**：被新一轮实战推翻的教训可以修订，但保留原结论演化痕迹（"~~原结论~~ → 新结论（R{n} 修正）"），经验库是活的但可追溯
7. **复盘产物优先**：如果你在自己的 loop 项目里维护了 leadbook.md，直接从中提炼通用条目——项目特定细节过滤掉

## 其他改动

- **全仓库匿名化**：任何文件不得出现真实项目名/硬件型号/OS 专有命令/私有路径/IP/账号——本技能要在任意领域项目上执行，具体语境会污染 agent 行为，且泄露私有信息
- SKILL.md 保持 ≤120 行（总纲克制，细节下沉 references/）；description 保持中英触发词并列，改一边同步另一边
- templates/ 四模板 `{FILL}` 占位风格一致，不新增同义变体；示例只进 example/，模板保持空骨架
- 脚本改动需 POSIX 兼容 + `sh -n` 过 + init.sh 干跑
- 新宿主映射（porting.md）：附实际验证说明，未验证的标"未验证"
- 提交信息：一行式说清楚改了什么为什么
